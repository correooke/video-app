import React from 'react';
import { View, Text } from 'react-native';

const Card = () => {
    // ejecución de función. 

    return (
        <View>
            <Text>
                CARD (EJEMPLO)!
            </Text>
        </View> 
    )
};

export default Card;